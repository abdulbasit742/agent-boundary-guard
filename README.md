# agent-boundary-guard
Zero-dependency CLI to audit AI agent repos for risky autonomy boundaries, wildcard permissions, broad filesystem roots, dangerous shell enablement, and hardcoded secrets in agent configs.
